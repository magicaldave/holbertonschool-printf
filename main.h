#ifndef MAIN_H
#define MAIN_H

/* standard libraries */
#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>
/* function files */
/* function prototypes*/
int _printf(const char *format, ...);
int printString(char *s);
#endif
